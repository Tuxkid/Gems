  Listing of: average.down
  Located in: /home/hrapgc/Rstuff/Gems
Last updated: 10/08/1999 
**************************************

"average.down" <-
function(x)
{
# uses fill.down() to average the missing values with ones present
    y <- fill.down(x)
    z <- rev(fill.down(rev(x)))
    xx <- (y + z)/2
}
